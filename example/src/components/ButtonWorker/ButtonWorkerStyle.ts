import { StyleSheet } from 'react-native';

export const ButtonWorkerStyle = StyleSheet.create({
  button: {
    margin: 15,
    padding: 15,
    minWidth: 200,
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#FF2436',
  },
  text: {
    fontSize: 16,
    color: '#FFFFFF',
    fontWeight: 'bold',
  },
});
